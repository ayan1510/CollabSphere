VITE_API_BASE=http://localhost:5000/api
# Replace with production backend URL on deployment if needed (otherwise defaults to the deployed Render URL)
# VITE_API_BASE=https://collabsphere-2-bs1e.onrender.com/api
